version: 2.1
jobs:
  build:
    docker:
      image: alpine:3.15
      auth:
        username: hiroakisuzuki
        password: $DOCKERHUB_PASSWORD

    steps:
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'

